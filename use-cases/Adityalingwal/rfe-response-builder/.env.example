# Copy to .env and fill in. Never commit .env.
SUPERDOCS_API_KEY=sk_your_key_here

# Hard cap on billable SuperDocs operations per run — one run being the whole
# cycle from draft to export, counted across its commands (dev-mode quota
# protection; an estimate, not the bill — see README)
MAX_OPS_PER_RUN=10
